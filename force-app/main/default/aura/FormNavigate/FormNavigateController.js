({
   /* 
    doInit : function(component, event, helper) {
        
        helper.getURLParams(component, event,helper);
        
    },
    
    
    next : function(component, event, helper) {
        debugger;
     //   var amount=component.get('v.ServIllustration');
     //   var sampleData={att1:"AAAAAAA",att2:"BBBBBB",att3:"CCCCC",att4:"DDDDDD"}
      //  helper.navigateNext(component, helper, "c:CMP_B",{dataObject:sampleData,ServIllustration:amount});
        
    },
    
    handleSectionToggle: function (cmp, event) {
        var openSections = event.getParam('openSections');
        
        if (openSections.length === 0) {
            cmp.set('v.activeSectionsMessage', "All sections are closed");
        } else {
            cmp.set('v.activeSectionsMessage', "Open sections: " + openSections.join(', '));
        }
    }
    */
})